# Dotfiles / 环境配置

一键部署个人 shell、编辑器与工具链配置。

## 版本

### v1.2 — 2026-09-10
- **修复**：Vim 配置的两处报错。
  - `SummerInfo.vim` 第 27 行残留的 `#        \printf("%s ", &filetype),` 导致
    `E488: Trailing characters`，已删除该行（`#` 不是 Vimscript 注释，且续行语法不完整）。
  - `setting.vim` 的 `set autochdir` 在精简版 vim（如 CentOS 7 的 `vim-minimal`）上
    报 `E518: Unknown option`，已用 `if exists('+autochdir')` 包裹，不支持的 vim 自动跳过。

### v1.1 — 2026-09-10
- **修复**：终端提示符错位问题。
  当系统 locale 为 `C`（`locale charmap` 返回 `ANSI_X3.4-1968`）时，
  提示符中的宽字符（如 emoji `🍉`、框线 `┌ └`）会被 readline 按字节计数，
  导致光标位置偏移、命令回显错乱（例如多出 `/S`）。
  现在通过 `export LC_CTYPE=en_US.UTF-8` 强制 UTF-8 字符处理，
  即使 `/etc/profile.d/lang.sh` 之后把 `LANG` 改回 `C` 也不受影响。

### v1.0 — 2026-09-10
- 初始版本。

## 使用方法

1. **修改偏好**
   编辑 `setting.sh`，按需修改自己的偏好配置。

2. **执行安装**
   ```bash
   sh setup.sh
